/**
 * CONNEXION DU SITE À SANITY — LE SEUL FICHIER À MODIFIER CÔTÉ SITE.
 *
 * Remplacez "replaceme" par le Project ID de votre projet Sanity
 * (le même que dans sanity/project.js).
 *
 * Tant que l'ID n'est pas renseigné, le site utilise content.json.
 */
window.SANITY = {
  projectId: "dvss1you",
  dataset: "production",
  apiVersion: "2024-10-01"
};
