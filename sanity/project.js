/**
 * IDENTIFIANT DU PROJET SANITY — LE SEUL FICHIER À MODIFIER.
 *
 * 1. Créez un projet gratuit sur https://www.sanity.io/manage
 * 2. Copiez son "Project ID" (8 caractères, ex. "ab12cd34")
 * 3. Remplacez la valeur ci-dessous.
 *
 * Le même ID doit être reporté dans js/sanity-config.js (côté site).
 */
export const projectId = 'dvss1you'
export const dataset = 'production'
